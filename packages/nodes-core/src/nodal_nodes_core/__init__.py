"""nodal 기본 노드 팩 — 유틸 · 수학 · 문자열 · 파일.

M1 에서 `@node` 데코레이터가 생기면 여기에 산술 노드부터 채운다
(M1 완료 기준: 산술 노드 10개짜리 그래프를 CLI로 실행).

이 패키지는 `nodal` 만 import 한다. server 를 import 하지 않는다.
"""

__all__: list[str] = []
__version__ = "0.0.0"
